<<<<<<< HEAD
# fileIndexer
 (Файловый индексатор и поисковый движок)
=======
File Indexer and Search Engine
Консольное приложение для индексации и поиска файлов по содержимому и названиям.

📋 Функциональность
Индексация содержимого директорий и поддиректорий

Поиск по имени файла с поддержкой частичного совпадения

Поиск по содержимому файлов с ранжированием по релевантности

Поддержка множества форматов: txt, docx, pdf, java, xml, json и другие

Автоматическое определение кодировки файлов

Статистика индексации и поиска

🚀 Быстрый старт
Сборка проекта
bash
# Клонирование репозитория
git clone <repository-url>
cd file-indexer

# Сборка с Maven
mvn clean compile assembly:single
Запуск приложения
Базовый запуск (для небольших директорий):

bash
java -jar target/file-indexer-1.0-SNAPSHOT-jar-with-dependencies.jar
⚙️ Рекомендации по запуску для разных сценариев
1. Стандартные директории (до 1000 файлов)
   bash
   java -Xms512m -Xmx2g -Dfile.encoding=UTF-8 -jar target/file-indexer-1.0-SNAPSHOT-jar-with-dependencies.jar
2. Большие директории (1000-10,000 файлов)
   bash
   java -Xms1g -Xmx4g -XX:+UseG1GC -Dfile.encoding=UTF-8 -jar target/file-indexer-1.0-SNAPSHOT-jar-with-dependencies.jar
3. Очень большие директории (10,000+ файлов)
   bash
   java -Xms2g -Xmx8g -XX:+UseG1GC -XX:+ExplicitGCInvokesConcurrent -Dfile.encoding=UTF-8 -jar target/file-indexer-1.0-SNAPSHOT-jar-with-dependencies.jar
4. Директории с огромными файлами (>100MB каждый)
   bash
   java -Xms4g -Xmx16g -XX:+UseG1GC -XX:MaxGCPauseMillis=200 -Dfile.encoding=UTF-8 -jar target/file-indexer-1.0-SNAPSHOT-jar-with-dependencies.jar
   📁 Поддерживаемые форматы файлов
   Текстовые форматы (полная индексация):
   .txt, .md, .rst, .log

.java, .py, .js, .css, .html, .xml, .json

.properties, .ini, .cfg, .conf, .yml, .yaml

.sql, .c, .cpp, .h, .cs, .php

Документы (ограниченная индексация):
.docx, .doc (только текст)

.pdf (только текст, если доступен)

.rtf, .odt, .epub, .fb2

Пропускаются:
Бинарные файлы: .exe, .dll, .zip, .rar, .jpg, .png

Временные файлы: ~$*, .tmp

Файлы больше 50MB

🖥️ Использование
Интерактивный режим (рекомендуется)
bash
java -jar target/file-indexer-1.0-SNAPSHOT-jar-with-dependencies.jar
Командная строка
bash
# Индексация директории
java -jar file-indexer.jar --index /path/to/directory

### Интерактивный режим (рекомендуется для новичков)

java -jar target/file-indexer-1.0-SNAPSHOT-jar-with-dependencies.jar


# Индексация нескольких директорий
java -jar file-indexer.jar --index /path/to/docs
java -jar file-indexer.jar --index /path/to/projects
java -jar file-indexer.jar --stats

# Индексация большой директории с мониторингом памяти
java -Xmx8g -jar file-indexer.jar --index /large/directory
java -jar file-indexer.jar --stats

# Поиск по имени файла
java -jar file-indexer.jar --search-name "report"

# Поиск по содержимому
java -jar file-indexer.jar --search-content "database"

# Очистка индекса
java -jar file-indexer.jar --clear

# Показать статистику
java -jar file-indexer.jar --stats
🎯 Примеры работы
Индексация
text
Choose action: 1
Enter the path to the directory for indexing: D:/мои документы
Starting indexing the directory: D:/мои документы
Supported extensions: [txt, java, xml, json, docx, pdf, ...]
Processing files... 100%
Indexing completed! Successfully saved: 1452 files
Поиск
text
Choose action: 2
Enter the file name to search for: отчет
Found 3 files:
1. финансовый_отчет.docx
   Path: D:/мои документы/финансовый_отчет.docx
   Size: 24567 bytes

Choose action: 3  
Enter text to search in content: Java programming
Found 15 files by relevance:
1. Tutorial.java (234 matches)
2. Guide.md (156 matches)
   ⚠️ Решение проблем
   Проблема: OutOfMemoryError (нехватка памяти)
   Решение: Увеличьте выделяемую память:

bash
java -Xmx8g -jar target/file-indexer-1.0-SNAPSHOT-jar-with-dependencies.jar
Проблема: Медленная индексация
Решение: Используйте более производительные настройки GC:

bash
java -Xmx4g -XX:+UseG1GC -XX:MaxGCPauseMillis=150 -jar target/file-indexer-1.0-SNAPSHOT-jar-with-dependencies.jar
Проблема: Кракозябры вместо русских букв
Решение: Явно укажите кодировку:

bash
java -Dfile.encoding=UTF-8 -Dsun.jnu.encoding=UTF-8 -jar target/file-indexer-1.0-SNAPSHOT-jar-with-dependencies.jar
Проблема: Файлы не индексируются
Проверьте:

Существует ли директория

Есть ли файлы с поддерживаемыми расширениями

Достаточно ли прав для чтения файлов

Не превышают ли файлы лимит размера (50MB)

📊 Оптимальные настройки памяти
Размер директории	Рекомендуемая память	Параметры запуска
< 1,000 файлов	2GB	-Xmx2g
1,000-5,000 файлов	4GB	-Xmx4g -XX:+UseG1GC
5,000-20,000 файлов	8GB	-Xmx8g -XX:+UseG1GC
> 20,000 файлов	16GB+	-Xmx16g -XX:+UseG1GC -XX:MaxGCPauseMillis=200
🔧 Дополнительные параметры JVM
Для максимальной производительности:

bash
java -Xms4g -Xmx8g \
-XX:+UseG1GC \
-XX:MaxGCPauseMillis=150 \
-XX:InitiatingHeapOccupancyPercent=35 \
-Dfile.encoding=UTF-8 \
-jar target/file-indexer-1.0-SNAPSHOT-jar-with-dependencies.jar
📁 Структура проекта
text
file-indexer/
├── src/
│   └── main/
│       ├── java/ru/gildina/indexer/
│       │   ├── FileIndexerApp.java          # Главный класс
│       │   ├── database/                    # Работа с БД
│       │   ├── service/                     # Сервисы индексации и поиска
│       │   └── model/                       # Модели данных
│       └── resources/
│           └── logback.xml                  # Конфигурация логирования
├── target/                                  # Собранные артефакты
├── file_indexer.db                         # База данных SQLite
└── logs/                                   # Логи приложения
💾 База данных
Приложение использует SQLite базу данных file_indexer.db:

Таблица files: информация о файлах (путь, размер, дата изменения)

Таблица file_contents: инвертированный индекс слов

Для очистки базы данных используйте опцию "Clean index" в меню или --clear в командной строке.

📝 Логирование
Логи сохраняются в папку logs/file-indexer.log. Уровень логирования можно настроить в src/main/resources/logback.xml.

🔄 Пересборка при изменении кода
bash
mvn clean compile assembly:single
❓ Получение помощи
bash
# Показать справку
java -jar target/file-indexer-1.0-SNAPSHOT-jar-with-dependencies.jar --help


Примечание: Для индексации очень больших директорий рекомендуется запускать приложение в фоновом режиме и периодически проверять использование памяти с помощью системных мониторов.
